# Word Search

Given a grid of characters and a word, determine if the word can be constructed from adjacent cells (no reuse).

## Example
Input: board = [["A","B","C","E"],...], word = "ABCCED"
Output: true
