# Find Median from Data Stream

Design a data structure that supports adding numbers and finding the median at any time.

## Example
add_num(1); add_num(2); find_median() -> 1.5; add_num(3); find_median() -> 2.0
