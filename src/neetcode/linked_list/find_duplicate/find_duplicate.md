# Find the Duplicate Number

Given an array of n+1 integers where each is between 1 and n, find the duplicate number without modifying the array or using extra space.

## Example
Input: nums = [1,3,4,2,2]
Output: 2
