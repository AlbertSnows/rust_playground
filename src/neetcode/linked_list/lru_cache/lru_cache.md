# LRU Cache

Design a data structure that follows the Least Recently Used cache eviction policy with O(1) get and put operations.

## Example
LRUCache(2); put(1,1); put(2,2); get(1)->1; put(3,3); get(2)->-1
