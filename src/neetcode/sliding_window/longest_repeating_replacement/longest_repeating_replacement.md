# Longest Repeating Character Replacement

Given a string and integer k, find the longest substring containing the same letter after replacing at most k characters.

## Example
Input: s = "ABAB", k = 2
Output: 4
