# Car Fleet

Given cars at positions with speeds heading to a target, return the number of car fleets that arrive.

## Example
Input: target = 12, position = [10,8,0,5,3], speed = [2,4,1,1,3]
Output: 3
