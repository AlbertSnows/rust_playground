# Min Stack

Design a stack that supports push, pop, top, and retrieving the minimum element in constant time.

## Example
MinStack ms; ms.push(-2); ms.push(0); ms.push(-3);
ms.get_min() -> -3; ms.pop(); ms.top() -> 0; ms.get_min() -> -2
