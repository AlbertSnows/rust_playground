# Count Good Nodes in Binary Tree

A node is "good" if no node on the path from root to that node has a greater value. Return the count of good nodes.

## Example
Input: root = [3,1,4,3,null,1,5]
Output: 4
