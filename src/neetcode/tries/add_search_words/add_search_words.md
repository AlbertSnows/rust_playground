# Design Add and Search Words Data Structure

Design a data structure supporting addWord and search where '.' matches any letter.

## Example
addWord("bad"); addWord("dad"); search(".ad") -> true; search("b..") -> true
